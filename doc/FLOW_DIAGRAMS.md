# FitFix Application Flow Diagrams

This document provides comprehensive flow diagrams for all major processes in the FitFix application.

---

## 📋 Table of Contents

1. [Authentication Flow](#1-authentication-flow)
2. [User Registration Flow](#2-user-registration-flow)
3. [Employee Signup Flow](#3-employee-signup-flow)
4. [Admin Workflow](#4-admin-workflow)
5. [Employee Workflow](#5-employee-workflow)
6. [User Workflow](#6-user-workflow)
7. [Chat System Flow](#7-chat-system-flow)
8. [Payment Flow](#8-payment-flow)
9. [Subscription Flow](#9-subscription-flow)
10. [Meal Plan Assignment Flow](#10-meal-plan-assignment-flow)
11. [Workout Plan Assignment Flow](#11-workout-plan-assignment-flow)
12. [Progress Tracking Flow](#12-progress-tracking-flow)
13. [System Architecture Flow](#13-system-architecture-flow)

---

## 1. Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Enter email & password
     ▼
┌─────────────────┐
│  Login Form     │
│  (Frontend)     │
└────┬────────────┘
     │
     │ 2. POST /api/auth/login
     │    { email, password }
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 3. Call Firebase Auth REST API
     ▼
┌─────────────────┐
│  Firebase Auth  │
└────┬────────────┘
     │
     │ 4. Returns idToken & uid
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 5. Fetch user profile from Firestore
     ▼
┌─────────────────┐
│  Firestore      │
│  users/{uid}    │
└────┬────────────┘
     │
     │ 6. Returns user data
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 7. Return { token, user, refreshToken }
     ▼
┌─────────────────┐
│  Frontend       │
│  Login Component│
└────┬────────────┘
     │
     │ 8. Store token in localStorage
     │ 9. Redirect based on role:
     │    - Admin → /admin-dashboard
     │    - Employee → /employee-dashboard
     │    - User → /dashboard
     ▼
┌─────────────────┐
│  Dashboard      │
└─────────────────┘
```

---

## 2. User Registration Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Fill registration form
     ▼
┌─────────────────┐
│  Register Form │
│  (Frontend)    │
└────┬────────────┘
     │
     │ 2. POST /api/auth/register
     │    { email, password, displayName, ... }
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 3. Create user in Firebase Auth
     ▼
┌─────────────────┐
│  Firebase Auth  │
└────┬────────────┘
     │
     │ 4. Returns userRecord with uid
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 5. Create user document in Firestore
     ▼
┌─────────────────┐
│  Firestore      │
│  users/{uid}    │
└────┬────────────┘
     │
     │ 6. Document created
     ▼
┌─────────────────┐
│  Backend API    │
│  authController │
└────┬────────────┘
     │
     │ 7. Return { success, user, token }
     ▼
┌─────────────────┐
│  Frontend       │
│  Register Form  │
└────┬────────────┘
     │
     │ 8. Auto-login & redirect to dashboard
     ▼
┌─────────────────┐
│  Dashboard      │
└─────────────────┘
```

---

## 3. Employee Signup Flow

```
┌──────────────┐
│  Applicant   │
└──────┬───────┘
       │
       │ 1. Fill employee signup form
       │    (with reCAPTCHA verification)
       ▼
┌──────────────────────┐
│  Employee Signup    │
│  Form (Frontend)    │
└──────┬───────────────┘
       │
       │ 2. POST /api/employee-requests
       │    { fullName, email, phone, plan, ... }
       ▼
┌──────────────────────┐
│  Backend API        │
│  employeeRequest    │
│  Controller         │
└──────┬───────────────┘
       │
       │ 3. Validate reCAPTCHA score
       │ 4. Check for duplicate email
       │ 5. Create request document
       ▼
┌──────────────────────┐
│  Firestore          │
│  employeeRequests   │
│  {requestId}        │
└──────┬───────────────┘
       │
       │ 6. Request created with status: 'pending'
       ▼
┌──────────────────────┐
│  Backend API        │
│  employeeRequest    │
│  Controller         │
└──────┬───────────────┘
       │
       │ 7. Create payment record
       ▼
┌──────────────────────┐
│  Firestore          │
│  employeePayments   │
│  {paymentId}        │
└──────┬───────────────┘
       │
       │ 8. Return success
       ▼
┌──────────────────────┐
│  Frontend           │
│  Signup Form        │
└──────┬───────────────┘
       │
       │ 9. Show success message
       │    "Request submitted, awaiting approval"
       ▼
       │
       │ [Admin Reviews Request]
       │
       ▼
┌──────────────────────┐
│  Admin Dashboard    │
│  (Admin)            │
└──────┬───────────────┘
       │
       │ 10. Admin reviews request
       │ 11. POST /api/admin/employee-requests/{id}/approve
       ▼
┌──────────────────────┐
│  Backend API        │
│  adminController    │
└──────┬───────────────┘
       │
       │ 12. Create employee in Firebase Auth
       │ 13. Create user document with role: 'employee'
       │ 14. Create subscription document
       │ 15. Send credentials email
       ▼
┌──────────────────────┐
│  Email Service       │
│  (Nodemailer)       │
└──────┬───────────────┘
       │
       │ 16. Email sent with login credentials
       ▼
┌──────────────────────┐
│  Applicant          │
│  (Now Employee)     │
└──────────────────────┘
```

---

## 4. Admin Workflow

```
┌─────────────────┐
│  Admin Login    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Admin Dashboard │
└──────┬──────────┘
       │
       ├──► Manage Employees
       │    ├── Create Employee
       │    ├── View All Employees
       │    ├── Update Employee Status
       │    └── Delete Employee
       │
       ├──► Manage Users
       │    ├── View All Users
       │    ├── View User Details
       │    └── Delete User
       │
       ├──► Employee Requests
       │    ├── View Pending Requests
       │    ├── Approve Request
       │    └── Reject Request
       │
       ├──► Subscriptions
       │    ├── View All Subscriptions
       │    ├── Check Expirations
       │    └── Renew Subscriptions
       │
       ├──► Payments
       │    ├── View All Payments
       │    ├── Approve Payments
       │    └── View Payment History
       │
       ├──► Employee Payments
       │    ├── View Payment Requests
       │    └── Approve Employee Payments
       │
       ├──► Analytics & Reports
       │    ├── Dashboard Statistics
       │    ├── User Reports
       │    ├── Payment Reports
       │    └── Subscription Reports
       │
       ├──► Chat
       │    ├── Chat with Employees
       │    └── Chat with Users
       │
       └──► System Management
            ├── View System Health
            └── Manage Settings
```

---

## 5. Employee Workflow

```
┌─────────────────┐
│ Employee Login  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│Employee Dashboard│
└──────┬──────────┘
       │
       ├──► Manage Clients (Users)
       │    ├── Create User
       │    ├── View My Users
       │    ├── Update User Info
       │    └── Delete User
       │
       ├──► Meal Plans
       │    ├── Create Meal Plan
       │    ├── Assign to User
       │    ├── Create Template
       │    └── View Templates
       │
       ├──► Workout Plans
       │    ├── Create Workout Plan
       │    ├── Assign to User
       │    ├── Add Exercises
       │    └── View Exercise Library
       │
       ├──► Track Progress
       │    ├── View User Progress
       │    ├── View Progress Photos
       │    └── Analyze Trends
       │
       ├──► Payments
       │    ├── View Payment Requests
       │    ├── Approve Payments
       │    └── Request Payment
       │
       ├──► Chat
       │    ├── Chat with Admin
       │    └── Chat with Users
       │
       ├──► Reports
       │    ├── My Users Report
       │    ├── Payment Report
       │    └── Subscription Status
       │
       └──► Profile
            ├── View Profile
            └── Update Profile
```

---

## 6. User Workflow

```
┌─────────────────┐
│   User Login    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  User Dashboard │
└──────┬──────────┘
       │
       ├──► View Meal Plans
       │    └── Assigned by Employee
       │
       ├──► View Workout Plans
       │    └── Assigned by Employee
       │
       ├──► Track Progress
       │    ├── Add Progress Entry
       │    ├── Upload Photos
       │    ├── Record Measurements
       │    └── View Progress History
       │
       ├──► Payments
       │    ├── Submit Payment
       │    └── View Payment History
       │
       ├──► Chat
       │    └── Chat with Employee
       │
       ├──► Notifications
       │    └── View Notifications
       │
       └──► Profile
            ├── View Profile
            └── Update Profile
```

---

## 7. Chat System Flow

```
┌─────────┐                    ┌─────────┐
│  User A │                    │  User B │
└────┬────┘                    └────┬────┘
     │                               │
     │ 1. Send Message              │
     │    POST /api/chat/send       │
     │    { recipientId, content } │
     │                               │
     ▼                               │
┌─────────────────┐                 │
│  Backend API    │                 │
│  chatController │                 │
└────┬────────────┘                 │
     │                               │
     │ 2. Generate Chat ID          │
     │    (role-based format)        │
     │                               │
     │ 3. Create/Update Chat Doc    │
     ▼                               │
┌─────────────────┐                 │
│  Firestore      │                 │
│  chats/{chatId} │                 │
└────┬────────────┘                 │
     │                               │
     │ 4. Add Message to Subcollection│
     ▼                               │
┌─────────────────┐                 │
│  Firestore      │                 │
│  chats/{chatId} │                 │
│  /messages/     │                 │
│  {messageId}    │                 │
└────┬────────────┘                 │
     │                               │
     │ 5. Create Notification        │
     ▼                               │
┌─────────────────┐                 │
│  Firestore      │                 │
│  notifications  │                 │
└────┬────────────┘                 │
     │                               │
     │ 6. Real-time Listener         │
     │    (Frontend)                 │
     │                               │
     │ 7. Message Appears            │
     │    in User B's Chat           │
     ▼                               ▼
┌─────────────────┐         ┌─────────────────┐
│  User A UI      │         │  User B UI      │
│  (Sent)         │         │  (Received)     │
└─────────────────┘         └─────────────────┘
```

**Real-time Updates**:
- Frontend uses `onSnapshot` listener on `chats/{chatId}/messages`
- Messages appear instantly without page refresh
- Unread count updates automatically

---

## 8. Payment Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Submit Payment
     │    POST /api/payments
     │    { amount, method, employeeId }
     ▼
┌─────────────────┐
│  Backend API    │
│  paymentController│
└────┬────────────┘
     │
     │ 2. Create payment document
     │    status: 'pending'
     ▼
┌─────────────────┐
│  Firestore      │
│  payments/{id}  │
└────┬────────────┘
     │
     │ 3. Notification sent to Employee
     ▼
┌─────────────────┐
│  Employee      │
│  Dashboard      │
└────┬────────────┘
     │
     │ 4. Employee Reviews Payment
     │    POST /api/payments/{id}/employee-approve
     ▼
┌─────────────────┐
│  Backend API    │
│  paymentController│
└────┬────────────┘
     │
     │ 5. Update payment
     │    approvedByEmployee: true
     ▼
┌─────────────────┐
│  Firestore      │
│  payments/{id}  │
└────┬────────────┘
     │
     │ 6. Notification sent to Admin
     ▼
┌─────────────────┐
│  Admin          │
│  Dashboard      │
└────┬────────────┘
     │
     │ 7. Admin Reviews Payment
     │    POST /api/payments/{id}/admin-approve
     ▼
┌─────────────────┐
│  Backend API    │
│  paymentController│
└────┬────────────┘
     │
     │ 8. Update payment
     │    status: 'completed'
     │    approvedByAdmin: true
     │ 9. Create admin transaction record
     ▼
┌─────────────────┐
│  Firestore      │
│  payments/{id}  │
│  adminTransactions│
└────┬────────────┘
     │
     │ 10. Notification sent to User
     ▼
┌─────────────────┐
│  User           │
│  (Payment Complete)│
└─────────────────┘
```

---

## 9. Subscription Flow

```
┌──────────────┐
│  Employee    │
│  Applicant    │
└──────┬───────┘
       │
       │ 1. Submit Payment
       │    POST /api/employee-payments
       │    { plan, amount, ... }
       ▼
┌──────────────────────┐
│  Backend API        │
│  employeePayment    │
│  Controller         │
└──────┬───────────────┘
       │
       │ 2. Create payment document
       │    paid: true
       │ 3. Create subscription document
       │    status: 'active'
       │    Calculate expiration date
       ▼
┌──────────────────────┐
│  Firestore          │
│  employeePayments   │
│  subscriptions      │
└──────┬───────────────┘
       │
       │ 4. Send confirmation email
       ▼
┌──────────────────────┐
│  Email Service       │
└──────┬───────────────┘
       │
       │ [Time Passes]
       │
       │ 5. Scheduled Job (Cron)
       │    Check expiring subscriptions
       ▼
┌──────────────────────┐
│  Scheduler          │
│  reminders.js       │
└──────┬───────────────┘
       │
       │ 6. Send expiration reminder
       │    (7 days before)
       ▼
┌──────────────────────┐
│  Email Service       │
└──────┬───────────────┘
       │
       │ [Subscription Expires]
       │
       │ 7. Scheduled Job
       │    Check expired subscriptions
       ▼
┌──────────────────────┐
│  Scheduler          │
│  subscriptionRenew   │
└──────┬───────────────┘
       │
       │ 8. Update subscription
       │    status: 'expired'
       │    isActive: false
       │ 9. Send expiration email
       ▼
┌──────────────────────┐
│  Firestore          │
│  subscriptions      │
└──────┬───────────────┘
       │
       │ 10. Employee can renew
       │     (Repeat from step 1)
       ▼
```

---

## 10. Meal Plan Assignment Flow

```
┌──────────────┐
│  Employee    │
└──────┬───────┘
       │
       │ 1. Create Meal Plan
       │    POST /api/employee/users/{userId}/meal-plans
       │    { title, meals, duration, ... }
       ▼
┌──────────────────────┐
│  Backend API        │
│  employeeController │
└──────┬───────────────┘
       │
       │ 2. Validate user exists
       │ 3. Validate user assigned to employee
       │ 4. Create meal plan document
       ▼
┌──────────────────────┐
│  Firestore          │
│  mealPlans/{id}     │
└──────┬───────────────┘
       │
       │ 5. Create notification
       ▼
┌──────────────────────┐
│  Firestore          │
│  notifications      │
└──────┬───────────────┘
       │
       │ 6. Notification appears
       │    in User's dashboard
       ▼
┌──────────────────────┐
│  User Dashboard      │
└──────┬───────────────┘
       │
       │ 7. User views meal plan
       │    GET /api/user/meal-plans
       ▼
┌──────────────────────┐
│  Backend API        │
│  userController     │
└──────┬───────────────┘
       │
       │ 8. Return user's meal plans
       ▼
┌──────────────────────┐
│  User Dashboard      │
│  (Meal Plan Display) │
└──────────────────────┘
```

---

## 11. Workout Plan Assignment Flow

```
┌──────────────┐
│  Employee    │
└──────┬───────┘
       │
       │ 1. Create Workout Plan
       │    POST /api/employee/users/{userId}/workout-plans
       │    { title, exercises, duration, ... }
       ▼
┌──────────────────────┐
│  Backend API        │
│  employeeController │
└──────┬───────────────┘
       │
       │ 2. Validate user exists
       │ 3. Validate user assigned to employee
       │ 4. Get exercises from exercises collection
       │ 5. Create workout plan document
       │    (Document ID = userId)
       ▼
┌──────────────────────┐
│  Firestore          │
│  workoutPlans/{userId}│
└──────┬───────────────┘
       │
       │ 6. Create notification
       ▼
┌──────────────────────┐
│  Firestore          │
│  notifications      │
└──────┬───────────────┘
       │
       │ 7. Notification appears
       │    in User's dashboard
       ▼
┌──────────────────────┐
│  User Dashboard      │
└──────┬───────────────┘
       │
       │ 8. User views workout plan
       │    GET /api/user/workout-plans
       ▼
┌──────────────────────┐
│  Backend API        │
│  userController     │
└──────┬───────────────┘
       │
       │ 9. Return user's workout plan
       │    (with exercise GIFs)
       ▼
┌──────────────────────┐
│  User Dashboard      │
│  (Workout Plan Display)│
└──────────────────────┘
```

---

## 12. Progress Tracking Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Add Progress Entry
     │    POST /api/user/progress
     │    { date, weight, measurements, photos, ... }
     ▼
┌─────────────────┐
│  Backend API    │
│  progressController│
└────┬────────────┘
     │
     │ 2. Validate user
     │ 3. Upload photos to Firebase Storage
     │ 4. Create progress document
     │    (in users/{userId}/progress subcollection)
     ▼
┌─────────────────┐
│  Firestore      │
│  users/{userId} │
│  /progress/{id} │
└────┬────────────┘
     │
     │ 5. Create notification for Employee
     ▼
┌─────────────────┐
│  Firestore      │
│  notifications  │
└────┬────────────┘
     │
     │ 6. Notification appears
     │    in Employee's dashboard
     ▼
┌─────────────────┐
│  Employee       │
│  Dashboard      │
└────┬────────────┘
     │
     │ 7. Employee views progress
     │    GET /api/employee/users/{userId}/progress
     ▼
┌─────────────────┐
│  Backend API    │
│  employeeController│
└────┬────────────┘
     │
     │ 8. Return user's progress history
     │    (with charts/analytics)
     ▼
┌─────────────────┐
│  Employee       │
│  Dashboard      │
│  (Progress View)│
└─────────────────┘
```

---

## 13. System Architecture Flow

```
┌─────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   React Web  │  │  Mobile App  │  │  Admin Panel │     │
│  │   (Vite)     │  │  (Future)    │  │  (React)     │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          │  HTTP/REST API   │                  │
          └──────────────────┼──────────────────┘
                             │
┌───────────────────────────▼───────────────────────────────┐
│                      API LAYER                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         Express.js Server (Node.js)                 │   │
│  │  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │   Routes      │  │  Middleware  │              │   │
│  │  │  - /api/auth  │  │  - Auth      │              │   │
│  │  │  - /api/admin │  │  - CORS      │              │   │
│  │  │  - /api/employee│ │  - Validation│              │   │
│  │  │  - /api/user  │  │              │              │   │
│  │  │  - /api/chat  │  │              │              │   │
│  │  └──────┬───────┘  └──────┬───────┘              │   │
│  │         │                 │                        │   │
│  │  ┌──────▼─────────────────▼───────┐              │   │
│  │  │      Controllers               │              │   │
│  │  │  - authController              │              │   │
│  │  │  - adminController              │              │   │
│  │  │  - employeeController          │              │   │
│  │  │  - userController               │              │   │
│  │  │  - chatController               │              │   │
│  │  │  - paymentController            │              │   │
│  │  └──────┬──────────────────────────┘              │   │
│  └─────────┼──────────────────────────────────────────┘   │
└────────────┼──────────────────────────────────────────────┘
             │
┌────────────▼───────────────────────────────────────────────┐
│                    SERVICES LAYER                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Firebase   │  │   Email      │  │   OpenAI     │     │
│  │   Admin SDK  │  │   Service    │  │   API        │     │
│  │              │  │  (Nodemailer)│  │  (AI Plans)  │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
┌──────────▼──────────────────▼──────────────────▼───────────┐
│                    DATA LAYER                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Firestore  │  │   Firebase   │  │   Firebase   │     │
│  │   Database   │  │   Auth       │  │   Storage    │     │
│  │              │  │              │  │              │     │
│  │  - users     │  │  - Login     │  │  - Photos    │     │
│  │  - chats     │  │  - Register │  │  - GIFs     │     │
│  │  - payments  │  │  - Tokens   │  │  - Files     │     │
│  │  - plans     │  │              │  │              │     │
│  │  - ...       │  │              │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└──────────────────────────────────────────────────────────────┘
          │
┌──────────▼──────────────────────────────────────────────────┐
│                    SCHEDULER LAYER                           │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │   Node-Cron  │  │   Reminders  │                        │
│  │              │  │   - Expiring │                        │
│  │  - Daily     │  │     Subscriptions│                    │
│  │  - Weekly    │  │   - Renewals │                        │
│  └──────────────┘  └──────────────┘                        │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Summary

1. **Client Request** → Express.js Server
2. **Authentication** → Firebase Auth (verify token)
3. **Authorization** → Middleware (check role)
4. **Business Logic** → Controller
5. **Data Access** → Firestore (read/write)
6. **Response** → Client (JSON)

---

## 📝 Notes

- All API endpoints are prefixed with `/api`
- Authentication uses JWT tokens from Firebase Auth
- Real-time updates use Firestore `onSnapshot` listeners
- File uploads go to Firebase Storage
- Scheduled tasks run via Node-Cron
- Email notifications sent via Nodemailer

---

**Last Updated**: Current Implementation
**Version**: 1.0.0

